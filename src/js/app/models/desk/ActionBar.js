
(function(define, APP) {

    'use strict';
    
    var dependencies = [
    ];

    define(dependencies, function() {
        
        var ActionBar = function(desk) {
            this.desk = desk;
            this.show = false;
            
        };
        
        ActionBar.prototype = {

        };

        return ActionBar;

    });

}(define, APP));
