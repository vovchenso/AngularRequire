
(function(define, APP) {

    'use strict';
    
    var dependencies = [
    ];

    define(dependencies, function() {
        
        var Settings = function(desk) {
            this.desk = desk;
            
        };
        
        Settings.prototype = {

        };

        return Settings;

    });

}(define, APP));
